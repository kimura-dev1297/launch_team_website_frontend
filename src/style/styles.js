const Header = {
	padding: '10px 20px',
	textAlign: 'center',
	color: 'white',
	fontSize: '22px'
};

const ErrorMessage = {
	color: 'white',
	fontSize: '13px'
};

const styles = {
	Header: Header,
	ErrorMessage: ErrorMessage
};

// Example use:  "import {styles} from "./styles";" then call styles.Header:
// <p style = {styles.ErrorMessage}>{this.state.error}</p>
